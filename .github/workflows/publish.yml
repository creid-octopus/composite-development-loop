# Release pipeline — pushes packages to Octopus and manages releases.
#
# Trigger behaviour:
#   main (push)       →  stable release (1.0.<run>), auto-deployed to Development
#   workflow_dispatch →  manual publish from any branch; feature branches produce
#                        pre-release packages (0.0.0-<slug>.<run>) and create a
#                        release in Octopus without auto-deploying — deploy from
#                        the Octopus UI when you're ready.
#
# This workflow intentionally does NOT trigger on plain feature branch pushes.
# Feature slot deployments are a separate, composable activity: push when you
# want to test, not on every commit.
#
# Required GitHub secrets (Settings → Secrets and variables → Actions → Secrets):
#   OCTOPUS_SERVER_URL  – e.g. https://your-instance.octopus.app
#   OCTOPUS_API_KEY     – service account key with package push + release create permissions
#   OCTOPUS_SPACE       – space name or ID (defaults to "Default" if omitted)
#
# Required GitHub variables (Settings → Secrets and variables → Actions → Variables):
#   OCTOPUS_PROJECT     – Octopus project name, e.g. "devloop-demo"

name: Publish

on:
  push:
    branches:
      - main

  # Manual dispatch — use this to push a feature branch to Octopus when you're
  # ready to test it in the feature deployment slot. Select the branch from the
  # "Run workflow" dropdown in the Actions tab.
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run — build and package only, skip all Octopus steps"
        type: boolean
        default: false
        required: false

# ── Workflow-level environment variables ────────────────────────────────────────
env:
  OCTOPUS_PACKAGE: devloop-demo-app
  VERSION_MAJOR: "1"
  VERSION_MINOR: "0"

jobs:
  publish:
    name: Build & publish
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ── Step 1: Compute version and build flags ──────────────────────────────
      # Branch routing logic: sets version, overwrite_mode, and is_release.
      #
      #   version        – semver string, e.g. "1.0.42" or "0.0.0-feature-foo.42"
      #   overwrite_mode – FailIfExists (main) | OverwriteExisting (feature)
      #                    Stable releases should never overwrite; feature builds
      #                    can because the version is "latest on this branch".
      #   is_release     – "true" only on main; gates the Deploy to Development step.
      - name: Compute build metadata
        id: meta
        run: |
          BRANCH="$GITHUB_REF_NAME"

          if [[ "$BRANCH" == "main" ]]; then
            VERSION="${{ env.VERSION_MAJOR }}.${{ env.VERSION_MINOR }}.${{ github.run_number }}"
            OVERWRITE_MODE="FailIfExists"
            IS_RELEASE="true"
          else
            SAFE_BRANCH=$(echo "$BRANCH" | sed 's|[^a-zA-Z0-9]|-|g' | tr '[:upper:]' '[:lower:]')
            VERSION="0.0.0-${SAFE_BRANCH}.${{ github.run_number }}"
            OVERWRITE_MODE="OverwriteExisting"
            IS_RELEASE="false"
          fi

          echo "version=$VERSION"               >> "$GITHUB_OUTPUT"
          echo "branch=$BRANCH"                 >> "$GITHUB_OUTPUT"
          echo "overwrite_mode=$OVERWRITE_MODE" >> "$GITHUB_OUTPUT"
          echo "is_release=$IS_RELEASE"         >> "$GITHUB_OUTPUT"

      # ── Step 2: Set up Node ──────────────────────────────────────────────────
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: src/package.json

      # ── Step 3: Install dependencies ────────────────────────────────────────
      - name: Install dependencies
        working-directory: src
        run: npm ci --omit=dev

      # ── Step 4: Stamp build metadata ────────────────────────────────────────
      - name: Write build metadata
        run: |
          cat > src/.build-env <<EOF
          APP_VERSION=${{ steps.meta.outputs.version }}
          APP_BRANCH=${{ steps.meta.outputs.branch }}
          APP_BUILD=${{ github.run_number }}
          APP_DEPLOYED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          APP_COMMIT_SHA=${{ github.sha }}
          EOF

      # ── Step 5: Package ──────────────────────────────────────────────────────
      - name: Package app
        id: package
        run: |
          PKG_NAME="${{ env.OCTOPUS_PACKAGE }}.${{ steps.meta.outputs.version }}.zip"
          cd src && zip -r "../$PKG_NAME" . -x "*.test.*" "*.spec.*"
          echo "pkg_path=$PKG_NAME" >> "$GITHUB_OUTPUT"

      # ── Step 6: Dry run summary (manual dispatch only) ───────────────────────
      # When dry_run is true, print everything that would have been sent to
      # Octopus — package name, version, overwrite mode, and whether a deploy
      # would have been triggered. Nothing is pushed or created.
      - name: Dry run summary
        if: inputs.dry_run == true
        run: |
          echo "╔══════════════════════════════════════════════════╗"
          echo "║              DRY RUN — nothing pushed            ║"
          echo "╚══════════════════════════════════════════════════╝"
          echo ""
          echo "  Package name   : ${{ env.OCTOPUS_PACKAGE }}"
          echo "  Package file   : ${{ steps.package.outputs.pkg_path }}"
          echo "  Version        : ${{ steps.meta.outputs.version }}"
          echo "  Branch         : ${{ steps.meta.outputs.branch }}"
          echo "  Commit SHA     : ${{ github.sha }}"
          echo "  Overwrite mode : ${{ steps.meta.outputs.overwrite_mode }}"
          echo "  Is release     : ${{ steps.meta.outputs.is_release }}"
          echo "  Octopus project: ${{ vars.OCTOPUS_PROJECT }}"
          echo ""
          if [[ "${{ steps.meta.outputs.is_release }}" == "true" ]]; then
            echo "  Deploy step    : WOULD run → environment: Development"
          else
            echo "  Deploy step    : WOULD skip (feature branch — manual deploy in Octopus)"
          fi
          echo ""
          echo "  Re-run without dry_run to push for real."

      # ── Step 7: Push package to Octopus ─────────────────────────────────────
      - name: Push package to Octopus
        if: inputs.dry_run != true
        uses: OctopusDeploy/push-package-action@v4
        with:
          server: ${{ secrets.OCTOPUS_SERVER_URL }}
          space: ${{ secrets.OCTOPUS_SPACE }}
          api_key: ${{ secrets.OCTOPUS_API_KEY }}
          packages: ${{ steps.package.outputs.pkg_path }}
          overwrite_mode: ${{ steps.meta.outputs.overwrite_mode }}

      # ── Step 8: Push build information ──────────────────────────────────────
      # Sends commit metadata to Octopus so releases can be traced back to the
      # exact commit that produced them.
      - name: Push build information
        if: inputs.dry_run != true
        uses: OctopusDeploy/push-build-information-action@v4
        with:
          server: ${{ secrets.OCTOPUS_SERVER_URL }}
          space: ${{ secrets.OCTOPUS_SPACE }}
          api_key: ${{ secrets.OCTOPUS_API_KEY }}
          packages: ${{ env.OCTOPUS_PACKAGE }}
          version: ${{ steps.meta.outputs.version }}

      # ── Step 9: Create Octopus release ──────────────────────────────────────
      - name: Create Octopus release
        if: inputs.dry_run != true
        uses: OctopusDeploy/create-release-action@v4
        with:
          server: ${{ secrets.OCTOPUS_SERVER_URL }}
          space: ${{ secrets.OCTOPUS_SPACE }}
          api_key: ${{ secrets.OCTOPUS_API_KEY }}
          project: ${{ vars.OCTOPUS_PROJECT }}
          git_ref: ${{ (github.ref_type == 'tag' && github.event.repository.default_branch) || (github.head_ref || github.ref) }}
          git_commit: ${{ github.event.after || github.event.pull_request.head.sha }}
          package_version: ${{ steps.meta.outputs.version }}
          release_number: ${{ steps.meta.outputs.version }}

      # ── Step 10: Deploy to Development (main only) ──────────────────────────
      # Skipped for feature branches — feature slot deployments are triggered
      # manually from the Octopus UI once you're ready to test.
      # Promotion through the lifecycle (Development → Staging → Prod) is
      # controlled by Octopus, not by additional steps here.
      - name: Deploy to Development
        if: inputs.dry_run != true && steps.meta.outputs.is_release == 'true'
        uses: OctopusDeploy/deploy-release-action@v4
        with:
          server: ${{ secrets.OCTOPUS_SERVER_URL }}
          space: ${{ secrets.OCTOPUS_SPACE }}
          api_key: ${{ secrets.OCTOPUS_API_KEY }}
          project: ${{ vars.OCTOPUS_PROJECT }}
          release_number: ${{ steps.meta.outputs.version }}
          environments: Development
