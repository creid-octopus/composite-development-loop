# Helm values for test.
# Overrides default values only for environment-specific settings.

namespace: devloop-demo-test

appEnv: Test

image: ghcr.io/creid-octopus/composite-development-loop:1.0.15-sha-4666fc7
