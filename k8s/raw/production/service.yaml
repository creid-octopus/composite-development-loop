apiVersion: v1
kind: Service
metadata:
  name: raw-devloop-demo
  labels:
    app: raw-devloop-demo
  namespace: devloop-demo-production
spec:
  type: LoadBalancer
  selector:
    app: raw-devloop-demo
  ports:
    - name: http
      port: 80
      targetPort: 3000
